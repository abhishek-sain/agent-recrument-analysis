# Use this workspace to collaborate
workspace:
  id: f2586c85-7d34-46aa-acb2-36cf3cd81d3f

localResources:
  collections:
    - ../POS - POS Referral Onboarding API
  specs:
    - ../openapi.json

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ../POS - POS Referral Onboarding API: pos-onboarding-api-collection
